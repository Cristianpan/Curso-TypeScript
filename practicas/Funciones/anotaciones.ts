/**
 * Se indica explícitamente el tipo del valor de retorno
 * 
 */
const sumar = (valor1: number, valor2: number): number => {
  return valor1 + valor2;
};

const dividir = function (a: number, b: number): number {
  return a / b;
};
